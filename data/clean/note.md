## Clean data directory 

This is where cleaned data should be stored.

The scripts you create to clean the data should be fully reproducible. That is, `raw data + data cleaning R scripts = clean data` without manual data manipulation.
Be careful not to override clean datasets if you can't reproduce them --- but this shouldn't be a problem if you keep the cleaning scripts updated.

The raw datasets should be stored in the `data/raw` directory.