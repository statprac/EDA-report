## Raw data directory 

This is where the raw data you are provided should be stored.

This data should remain read-only and **never overwritten**.

Cleaned datasets derived from the raw data should be stored in the `data/clean` directory.
